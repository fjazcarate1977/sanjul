{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^6.0.3",
    "npm-run-all": "^4.1.5",
    "typescript": "^4.7.4"
  },
  "scripts": {
    "lerna-bootstrap": "lerna bootstrap",
    "lerna-dev": "lerna run dev --parallel",
    "lerna-build": "lerna run build",
    "lerna-up": "run-s lerna-bootstrap",
    "lerna-up-prod": "run-s lerna-bootstrap lerna-build"
  }
}
